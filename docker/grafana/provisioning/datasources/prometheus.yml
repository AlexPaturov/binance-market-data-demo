# Datasource задаётся файлом, а не кликами — чтобы дашборд был воспроизводим из репозитория.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://bdc_prometheus:9090
    isDefault: true
    editable: false
