# 🚫 README_DO_NOT_TOUCH

Этот документ — **предохранитель**.
Он существует, чтобы через полгода ты не сказал: «кто это сделал и зачем».

Если ты читаешь это перед изменениями в проде — **остановись и дочитай до конца**.

---

## ❗ Общий принцип

> Если что-то **работает в PROD**, и ты **не понимаешь на 100% зачем оно так сделано** —
> **НЕ ТРОГАЙ**.

Большинство пунктов ниже ломают прод **не сразу**, а через часы или дни.

---

## 🗄 External Volumes — ТРОГАТЬ ЗАПРЕЩЕНО

Следующие volume являются **контрактом данных**:

* `binancecollector_postgres_data`
* `binancecollector_seq_data`
* `binancecollector_rabbitmq_data`
* `binancecollector_letsencrypt_data`
* `binancecollector_bdc_data`

### 🚫 НЕЛЬЗЯ:

* удалять (`docker volume rm`)
* пересоздавать
* менять `name:`
* делать их non-external

### Почему:

* потеря данных
* потеря логов
* потеря сертификатов
* неконсистентное состояние Hangfire

---

## 🌐 Docker Networks — зона повышенной опасности

### `binancecollector_web`

* используется Traefik
* используется Cloudflare Tunnel
* **external network**

🚫 НЕ МЕНЯТЬ:

* имя
* тип
* external=true

❗ Ошибка в сети = Traefik перестаёт маршрутизировать.

---

## 🔌 Порты сервисов

### Worker / DataManager

🚫 **НИКОГДА не публиковать порты напрямую** (`ports:`)

Разрешено:

* доступ **только через Traefik**

Почему:

* ломается TLS
* ломается Cloudflare
* нарушается модель безопасности

---

## 🧭 Traefik labels

🚫 НЕ ТРОГАТЬ без понимания:

* `traefik.http.routers.*`
* `traefik.http.services.*`
* `traefik.docker.network`

Ошибка в label = сервис пропадает из интернета.

---

## 🧪 DEV vs PROD

🚫 НЕ:

* запускать `docker-compose.dev.yml` на сервере
* смешивать dev и prod compose файлы

Всегда:

* DEV — локально
* PROD — только `docker-compose.prod.yml`

---

## 🔐 Secrets и env

🚫 НЕЛЬЗЯ:

* коммитить `.env`
* логировать secrets
* дублировать secrets в appsettings

Если сервис стартует без нужной env — **это нормально**, а не баг.

---

## 🧠 HealthChecks

HealthChecks:

* показывают **жизнь процесса**
* НЕ гарантируют бизнес-работоспособность

🚫 Не пытайся чинить «зелёную лампочку», если проблема в логике.

---

## 📜 Логи (Seq)

Seq — источник истины для:

* стартов
* падений
* деградаций

🚫 Не удаляй volume Seq
🚫 Не чисти данные без причины

---

## 🛑 Если очень хочется что-то поменять

Перед изменением задай себе 3 вопроса:

1. Что именно я хочу улучшить?
2. Что может сломаться?
3. Как откатиться?

Если нет чёткого ответа — **не меняй**.

---

## ✅ Итог

Этот файл — не запрет ради запрета.
Это **контракт с будущим собой**.

Если ты читаешь это — значит, он сработал.
